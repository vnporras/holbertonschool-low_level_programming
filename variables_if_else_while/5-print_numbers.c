#include <stdio.h>
/**
 * main - This is the main function for 5-print_numbers.c.
 * Return: Description of the returned value
 */
int main(void)
{
char number;
for (number = 0; number < 10 ; number++)
{
putchar(number);
}
return (0);
}
